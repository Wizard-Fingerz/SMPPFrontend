/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['wizcode28.pythonanywhere.com'], // Add your image domain(s) here
    },
    reactStrictMode: true, // Enables React strict mode for additional checks and warnings
    // Other Next.js configuration options can be added here
  };
  
  export default nextConfig;
  